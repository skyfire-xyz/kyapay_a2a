[project]
name = "kyapay-a2a"
version = "1.0.0"
description = "Skyfire KyaPay payment protocol extension for A2A"
readme = "README.md"
license = { file = "LICENSE" }
authors = [
    { name = "Skyfire", email = "support@skyfire.xyz" },
    { name = "Google A2A", email = "a2a@google.com" }
]
requires-python = ">=3.10"
keywords = ["a2a", "kyapay", "payments", "skyfire", "google"]
dependencies = [
    "a2a-sdk>=0.1.0",  # For A2A protocol integration
    "python-jose[cryptography]>=3.3.0",  # For JWT verification
    "validators>=0.20.0",  # For email validation
    "httpx>=0.28.1",  # For async HTTP client
    "pydantic>=2.0.0",  # For data validation
    "requests>=2.31.0",  # For JWKS fetching
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
    "pytest-asyncio>=1.1.0",
    "pytest-cov>=6.2.1",
    "pytest-mock>=3.14.1",
    "trio>=0.22.0",
]
lint = [
    "ruff>=0.13.1",
    "mypy>=1.18.0",
]

[project.scripts]
test = "pytest:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kyapay_a2a"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "unit: Unit tests",
    "integration: Integration tests", 
    "async: Async tests"
]

[tool.ruff]
target-version = "py310"

[tool.ruff.lint]
# Ignore F401 (unused import) in all `__init__.py` files
per-file-ignores = { "__init__.py" = ["F401"] }

[tool.mypy]
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true
strict_equality = true
